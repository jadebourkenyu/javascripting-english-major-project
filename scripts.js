var numbers = [1,2,3]
var doubles = nmbers.map(function(x){
  return x*2;
})

if (turtle.name.includes("o")){
  return turtle.name;
}
// this is not a good idea because it is an easy way out and won't work for everyt case.If you hade a list of things this wouldn't work because your list migh contain every letter. 
